<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="JoinMapper">
	<select id="idCheck" parameterType="java.lang.String" resultType="int">
		select count(id) cnt
		from member
		where id = #{id}
	</select>
	<insert id="formSubmit" parameterType="kr.co.htgather.join.MbrDTO">
		insert into member(id,pwd,name,gender, email, tel,weight, height, joindate)
		values (#{id},#{pwd},#{name},#{gender},,#{email},#{tel},#{weight},#{height},now())
	</insert>
	
</mapper>











